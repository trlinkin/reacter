---
sources:
  all:
    dummy.counter:
      persistent: false

      actions:
        exec: /usr/local/bin/send_neon

      when:
        critical:
          above: 90

        warning:
          above: 75